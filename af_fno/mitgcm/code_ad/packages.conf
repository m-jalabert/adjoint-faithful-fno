# AF--FNO adjoint packages.  The forward set (af_fno/mitgcm/code/packages.conf)
# plus the AD stack.  MNC is omitted; all products use global MDS.
#
# diagnostics is retained so the adjoint executable also writes the forward
# daily dynState/surfState snapshots, making each adjoint run self-documenting
# about the trajectory it linearised around.
gfd
diagnostics

autodiff
cost
ctrl
grdchk
