# Model C pushforward duration diagnosis

Classification: `training_only_pushforward_gate_not_yet_passed`.

The figure and CSV compare pushforward-v1 step 1,920 and all eight duration checkpoints with persistence and training climatology. Solid ratio curves use persistence and dotted curves use climatology; values below one win.

The original pushforward path replayed bitwise exactly. This experiment used split 1 only and did not authorize replication or validation.
