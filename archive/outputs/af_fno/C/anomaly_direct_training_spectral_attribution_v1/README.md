# Model C training-only pressure spectral attribution

This package uses only trajectory-v2 split 1 and the three accepted pointwise-anomaly/direct-state checkpoints. It does not read inference, response, or adjoint state.
