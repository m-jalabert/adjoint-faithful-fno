# Corrected Model C training-only pressure spectral attribution

This v2 package uses a ten-daily dataset offset per autoregressive model call. The v1 package is retained as an invalid temporal-alignment diagnostic and is not scientific evidence.
