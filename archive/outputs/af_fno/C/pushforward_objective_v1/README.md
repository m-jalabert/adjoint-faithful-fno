# Model C pushforward objective

Classification: `training_only_pushforward_gate_not_yet_passed`.

The figure and CSV compare the source and all four fine-tune checkpoints with persistence and training climatology. Solid ratio curves use persistence and dotted curves use climatology; values below one win.

This experiment used split 1 only and did not authorize replication or validation.
